database:
  namespace: test-platform

mongodb:
  enabled: true
  rootUser: root
  username: testuser
  database: testdb
  pvc:
    name: mongo-pvc
    size: 10Gi
  service:
    type: NodePort
    port: 27017
    nodePort: 30018